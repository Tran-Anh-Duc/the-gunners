<?php

namespace App\Helpers;

use Carbon\Carbon;

/**
 * Helper người dùng.
 *
 * File này hiện chưa được dùng trong flow chính của MVP.
 * Nó được giữ lại như chỗ dự phòng cho các helper liên quan tới user,
 * thay vì rải logic tiện ích sang nhiều nơi khác nhau.
 */
class UserHelper
{
//    public function hasPermission($permissionName)
//    {
//        return $this->getAllPermissions()
//            ->pluck('name')
//            ->contains($permissionName);
//    }
//
//    public function hasRole($roleName)
//    {
//        return $this->roles()->where('name', $roleName)->exists();
//    }
}
